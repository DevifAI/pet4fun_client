import AccountContainer from "./areas/AccountContainer";
import AccountHeader from "./areas/AccountHeader";

const Account = () => {
  return (
    <div>
      <AccountHeader />
      <AccountContainer />
    </div>
  );
};

export default Account;
